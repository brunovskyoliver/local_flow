# T001–T014 verification sweep

The original verification sweep is updated below for the authorized T004/T005/T011 work and
the T007 privacy regression. Evidence describes implementation and deterministic tests only;
authentic corpus, human review and hardware acceptance remain separate.

| Task | Status | Evidence |
| --- | --- | --- |
| T001 | Complete | `acceptance/baseline.md` records commit `4680c54…`, dirty state, FluidAudio 0.15.7 / GRDB 7.10.0 / Parakeet v3 pins and the constitution scope check. Both recorded hashes re-verified today: `fixtures/audio/manifest.json` → `007d678e…`, `scripts/dictation-accuracy.py` → `e0ef338e…`. Both files are unmodified in `git status`. |
| T002 | Complete | `fixtures/quality/README.md` documents private locations, explicit retention, rights verification, 0700/0600 modes and content-free console output. `.gitignore` covers `build/`, `*.wav`/`*.m4a`/`*.caf`, `fixtures/quality/private/` and `quality-runs/`. Only public FLEURS metadata is tracked. |
| T003 | Complete | Two v1 quickstart runs in `build/quality-002-baseline-a` and `-b`, each with results, historical-scorer report and build log. All 30 fixture IDs tabulated in `baseline.md` with text/window/completeness differences and both results/report hashes. Identical report hashes across runs. |
| T004 | Complete | `fixtures/quality/assembly-cases.json`: 24 authored cases, exact raw UTF-8 hex, mapped source spans/times, expected text/words, completeness reasons and insertion eligibility. Covers unique/phrase overlaps, repetitions, conflicting suffixes, competing anchors, both switch directions, partial words, punctuation/case, technical/IP tokens, contiguous segments, gaps, missing/empty/invalid timing, invalid sequence and padding-only evidence. Corpus consistency tests pass. No assembler is implemented or claimed tested. |
| T005 | Complete | `fixtures/quality/normalization-cases.json`: 45 authored positive/negative cases for N001–N006/V001, explicit canonical entity spellings, protected spans, diacritics, unchanged capitalization/artifacts, alias conflicts including disabled entries, ambiguous overlaps, adjacent replacements, rejected cycles and capacity expansion. Meaning verdicts remain null. No normalizer is implemented or claimed tested. |
| T006 | Complete | `scripts/test-transcription-quality.py`, 12 tests, all passing. Mapped to the task's enumerated conditions: tie precedence and empty references (`test_tie_and_empty`), CER (`test_tie_and_empty`, asserts `'č'` vs `'c'` → 1.000000), weighted aggregates (`test_weighted`), punctuation/diacritics/numbers (`test_tokenizer`), bounds (`test_bounds`), failed/missing/oversized outputs (`test_failed_missing_and_oversized`, `test_failed_recovered_text`), overlapping categories (`test_weighted`, asserts a fixture in `english`+`technical` aggregates identically into both), stale reviews and manifest mismatch (`test_mismatch_and_review`, `test_hash_review`), private review creation (`test_private_creation`), acceptance exit codes and console privacy (`test_cli_exit_and_privacy`), technical alignment (`test_exact_technical_alignment`). |
| T007 | Complete; privacy gap closed | Existing fake-runtime tests plus `testEvaluatorHasNoTranscriptLoggingOrAttachments` in `QualityEvaluationTests.swift` (9 tests total). The added Swift source regression rejects diagnostic/attachment sinks in both evaluator helpers and the opt-in entry, and checks its fixed noninterpolated failure message. This audits repository-owned evaluation code, not SDK internals. `make check` passes. |
| T008 | Complete | v2 validation in `scripts/transcription-quality.py` (`manifest()`, `safe_id`, `digest`, `bounded_text`, `read`) and `QualityEvaluationArtifacts.swift` (`QualityFixture.validate`). All enumerated bounds present and enforced before allocation: safe IDs ≤128 bytes, 4,194,304-byte artifacts, 256 fixtures, 768 reviews, 4,096-byte explanations, plus provenance/partition/switch/technical fields, explicit stage identities and nullable measurements with reasons. |
| T009 | Complete | Optional `evidenceObserver` on `FluidAudioEngineFactory`/`FluidAudioRuntime` capturing SDK text and original timings before word building and clamping; nonfinite timings encoded as tagged invalid values (`RecognitionEvidence.Timing`). Default is `nil`, so the production path is byte-identical when disabled. Enabled-observer evidence is stronger than required: the v2 runs with the observer active reproduced the v1 Slovak (10.582%) and synthetic-mixed (23.869%) rates exactly, so the observer changes no text. No join or recognition configuration was modified. Sequenced after T003 as required. |
| T010 | Complete | `QualityEvaluationRunner.swift`: preflight hashes and format-checks every fixture before any runtime is acquired, 1,048,576-byte hashing chunks, 1,600-frame decoding, ≤32 trace windows with per-window text/token bounds, artifact and run-budget limits in `QualityArtifacts`. Pending ledger persisted before inference; result written with `RENAME_EXCL` and fsync before the ledger advances; `recover()` refuses to reopen a complete or interrupted run. |
| T011 | Complete | `scripts/quality-gates.py` evaluates SC-001–SC-008 from bound evidence, computed counts, actual rescoring/repeated-run comparison and reviewed external findings. `score` accepts `--evidence`, `--baseline`, `--baseline-reviews`, `--repeat`. Missing/stale evidence cannot pass; observed failures fail; reviewed retention makes only SC-007 not applicable and never passes mixed accuracy by assertion. `scripts/test-transcription-quality.py` has 25 passing tests including all-pass synthetic evidence, each applicable failure, stale evidence/case hashes, threshold edges, adoption/fallback, tuning exclusion, CLI repetition/rescoring and private Git ignores. See `contracts/acceptance-evidence.md`. Real acceptance remains open. |
| T012 | Complete | `testOptInQualityFixtures` in `RuntimeCompatibilityTests.swift`, using the existing model/fixture variables plus `LOCALFLOW_QUALITY_OUTPUT`; skips without all four inputs; v1 `testOptInSpeechFixtures` preserved at line 88; automatic language with no hint; lifecycle owns the model; normalization and vocabulary reported as `unavailable_historical_stage_absent` rather than fabricated. |
| T013 | Complete | 80 public clips plus 30 legacy fixtures frozen in `fixtures/quality/public-selection-lock.json`. Reconstruction reproduced all audio and exact manifest bytes; source rights and conversion provenance recorded in `fixture-acquisition.md` and `dataset-rights.md`. Common Voice is optional; authentic switching remains a separate gap. |
| T014 | Complete | Two full 110-fixture runs, two identical rescoring passes per run, zero exact result/window/stage/status differences. Both runs have 95 complete and 15 incomplete results, all scored. Per-category weighted WER/CER, denominators, source/run hashes and limitations are in `baseline.md`. |

## Public baseline follow-up

T013/T014 now use the owner's revised public-corpus criteria. No private recording is required. Two complete 110-fixture runs reproduce exact raw windows, token timings, assembled text, completeness and failure reasons. Fifteen incomplete transcriptions remain in the denominator. Baseline completion does not establish full-feature quality acceptance.

The original 30-fixture subset still tops out at 25.75 seconds. New longer Slovak clips range from 15.12 to 21.56 seconds. Authentic within-speaker switching and natural 60–180-second speech remain coverage gaps. Human reviews, normalization, vocabulary, signed-app acceptance and resource measurements remain outstanding.

The acquisition regression suite passes all ten tests. `make check` passes after the public baseline runs, including 25 quality-scoring tests and deterministic XCTest. The default repository check skips opt-in inference without explicit inputs; both full baseline inference runs were executed separately with provisioned assets.

Constitution check: acquisition is explicit development tooling; no new production dependency, runtime owner, server or shared-wire change. Audio and reference-bearing artifacts remain local. No recognition setting, historical assembly rule, scorer normalization or acceptance threshold changed in this follow-up. No architecture exception or ADR is required. No VoiceInk source was used.

## T015 continuation, 2026-09-17

Reverified the latest acquisition/evaluation edits before implementation. All current frozen T013/T014 artifact hashes and all 110 paired result files match; acquisition validation and `make check` pass. No reproducibility defect or baseline change was needed.

T015, the standalone T021 implementation and the T025 replay are complete. [Assembly evidence](assembly.md) records the 24-case authoritative corpus, capacity/failure tests, 230 passing XCTest tests, repeated frozen-window replay, raw/assembled category metrics and every regression. Raw evidence remains unchanged and separately recoverable. Production envelope/storage/coordinator integration remains open. Exact next task: T016 storage/migration/recovery contract tests.
