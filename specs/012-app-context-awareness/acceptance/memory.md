# Memory with context on

Status on 2026-09-24: **unmeasured.** The Feature 001 memory protocol (idle and recording RSS of the signed app) was not rerun with context on. The design bound is one snapshot of at most 8,192 bytes plus its bounded parts per dictation, with no cache or queue; that bound is not a measurement.
