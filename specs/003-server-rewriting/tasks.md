# Tasks: server-assisted dictation rewriting

**Input**: Design documents in `specs/003-server-rewriting/`.
**Prerequisites**: `plan.md`, `spec.md`, `research.md`, `data-model.md`, `quickstart.md` and the three files in `contracts/`.
**Tests**: Required by the specification (FR-022 contract scenarios, FR-023 quality checker, SC-001 to SC-004, SC-008, SC-010, SC-011) and by constitution principle 12. Write the deterministic tests before the code they exercise, confirm they fail for the intended reason, then make them pass. Live-server, quality review, latency and memory evidence are separate acceptance tasks and are never marked done from fakes.
**Organization**: Setup, foundational protocol/storage/transport work, one phase per user story in priority order, a reference-server phase, then quality, acceptance and polish. All paths are relative to the repository root. New Swift files stay in the existing `LocalFlow` and `LocalFlowTests` targets and are registered in `apps/macos/LocalFlow.xcodeproj/project.pbxproj`.

`[P]` marks tasks that touch different files and can run alongside the other `[P]` tasks in the same phase. It never bypasses a phase gate. All tasks start unchecked; planning establishes no implementation or acceptance completion.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1 to US7)
- Every task names the file(s) it changes

## Phase 1: Setup

**Purpose**: Record the starting point, prepare private-output handling and register the files the plan introduces so later phases only fill them in.

- [X] T001 Record the implementation starting commit, dirty-tree state, Go/Xcode/GRDB/FluidAudio pins, the `llama-server` path and version on the reference machine, and the constitution scope check in `specs/003-server-rewriting/acceptance/baseline.md`; note that no latency, quality or memory figure exists yet.
- [X] T002 [P] Add `fixtures/rewrite/private/` and `build/rewrite-quality-*/` to `.gitignore`, and create `fixtures/rewrite/README.md` describing the public corpus, the private overlay format, 0700 output directories and the rule that runner output is never committed.
- [X] T003 [P] Add `NSAppTransportSecurity` with `NSAllowsArbitraryLoads = true` and an XML comment naming the reason (single user-chosen self-hosted endpoint; client enforces credential and insecure-override policy) to `apps/macos/LocalFlow/Info.plist`; record in `specs/003-server-rewriting/acceptance/baseline.md` that `NSAllowsLocalNetworking` alone is to be re-checked against the owner's overlay-network address during the T073 connection-test walkthrough.
- [X] T004 Register the new source and test files from the plan's project structure (`Core/Rewrite/*.swift`, `Features/Rewrite/RewriteCoordinator.swift`, `LocalFlowTests/Rewrite*Tests.swift`) as empty placeholders in `apps/macos/LocalFlow.xcodeproj/project.pbxproj` so `plutil -lint` and the Xcode build stay green while phases fill them in.

## Phase 2: Foundational prerequisites

**Purpose**: Protocol types, storage, settings snapshot, transport, latency capture and metrics that every story depends on. Complete this phase before starting any story phase. No user-visible behavior changes yet; with the rewrite dependency nil, the app is byte-for-byte the Feature 002 app.

### Protocol (delivery step 1)

- [X] T005 [P] Add `protocol/schemas/rewrite-request.schema.json` (fields `schema_version` const 1, `request_id` UUID, `mode` enum `clean|polished|concise`, `text` 1…20,000 scalars, `language_hints` array ≤ 4 BCP-47 strings, `stream_deltas` boolean, `additionalProperties: false`) and `protocol/schemas/rewrite-event.schema.json` (oneOf `accepted`, `progress`, `delta`, `result`, `error` with the required `result` identity fields `server`, `backend`, `prompt_version`, `shield`, `timing`) per `contracts/rewrite-protocol.md`.
- [X] T006 [P] Add `GET /v1/rewrite/health` and `POST /v1/rewrite` (response `application/x-ndjson`, status codes 400/401/403/413/429/503 with `{"error":{"code","message"}}`) to `protocol/openapi.yaml`, referencing the two schemas, and list the rewrite protocol in `protocol/README.md`.
- [X] T007 [P] Write failing tests in `apps/macos/LocalFlowTests/RewriteProtocolTests.swift` for every client validation rule in `contracts/rewrite-protocol.md`: request encodes only the six permitted fields; `oversized_response` at `min(4 × input_bytes + 8,192, 73,728)` bytes; `malformed_response` on non-object line, missing `event`, mode mismatch, missing identity or timing fields, missing terminal event; `unsupported_schema_version`; `request_mismatch`; `empty_response` on blank text; result text cap `min(4 × input_bytes, 65,536)`; `server_validation_failed` when `⟦` or `⟧` remains; second terminal event ignored; `unchanged` recomputed locally; server error-code mapping per `contracts/rewrite-protocol.md` (`shield_restore_failed`/`backend_error` → `server_validation_failed`, `output_too_large` → `oversized_response`, both backend timeouts → `backend_unavailable`, 401/403 → `authentication_failed`, 413 `too_large` → `server_validation_failed`, 429 `server_busy` → `backend_unavailable`, 503 → `backend_unavailable`); a server response never maps to a pre-admission reason, since the request was already admitted.
- [X] T008 Implement `apps/macos/LocalFlow/Core/Rewrite/RewriteProtocol.swift`: `RewriteMode` (`exact`, `clean`, `polished`, `concise`), `RewriteRequest` with a closed `CodingKeys` set, `RewriteEvent` enum, `RewriteResult`, `HealthResponse`, `RewriteFailureCategory` as one enum split into the twelve persisted post-admission codes and the seven pre-admission refusal reasons from `data-model.md` (with `isPersistable`), `RewriteResultValidator.validate(events:for:inputBytes:)` implementing rules 1–9 in order, and `RewriteNotice.text(for:context:)` mapping every category to the exact notice strings in `contracts/client-rewrite.md` with the live/history variant; make T007 pass.
- [X] T009 [P] Add Go types and validation in `server/internal/rewrite/protocol.go` (`Request`, `Event`, `Result`, `Health`, `ErrorCode` including `output_too_large`, `DecodeRequest` rejecting unknown fields, wrong `schema_version`, invalid UUID, invalid mode, blank text, text over 20,000 scalars or 65,536 bytes, more than 4 hints, non-boolean `stream_deltas`) with table tests in `server/internal/rewrite/protocol_test.go` covering each rejection and a round-trip of each event type.

### Storage (delivery step 2)

- [X] T010 [P] Write failing tests in `apps/macos/LocalFlowTests/RewriteStoreTests.swift`: migration `rewrite-v4` creates `rewrite_attempts` with the columns and checks in `data-model.md`, adds `transcriptions.rewrite_state` default `not_requested`, `delivered_source` and `delivered_rewrite_attempt_id` with `ON DELETE SET NULL`; ordinals are 1-based and unique per dictation; the eleventh `begin` throws `attempt_limit`, writes nothing and leaves ten rows; `begin` while a `pending` row exists for the dictation throws `concurrency_limit` and writes nothing (the in-transaction re-check of admission steps 4–5 in `data-model.md`); `input_hash` is the SHA-256 of `input_text`; `recordResult` stores `output_text`, `output_hash`, `unchanged`, identity and span columns; `recordFailure` accepts only the twelve persisted post-admission codes (the check constraint rejects a pre-admission reason); `recordFailure` and `recordCancelled` leave `output_text` null; `markStale` sets `stale = 1` without changing state; `rewrite_state` mirrors the newest attempt in the same transaction and stays `not_requested` when no row was ever admitted; `cancelPendingOnStartup` turns `pending` into `failed/interrupted`; `deleteConfirmed` cascades to zero attempt rows; a pre-003 database opens and reads `not_requested` with no attempts; `payload_bytes` reserves `length(input_text) + min(4 × length(input_text), 65,536)` on `begin`, adjusts on success, releases on failure, and `begin` throws `capacity_exceeded` with no row inserted and no ordinal consumed when the 33,554,432-byte quota does not fit; startup reconciliation counts attempt bytes.
- [X] T011 Add migration `rewrite-v4` to `apps/macos/LocalFlow/Core/Storage/HistoryMigrations.swift` exactly as specified in `data-model.md` (table, unique index on `(transcription_id, ordinal)`, check constraints on `mode`, `state`, `failure_category` limited to the twelve persisted post-admission codes, hash lengths, `≤ 65,536`-byte text columns, `≤ 128`-byte identity columns, `≤ 255`-byte `endpoint_origin`).
- [X] T012 Add `RewriteAttempt` (persisted record, `RewriteAttemptState`, latency span fields, identity block, `delivered`, `stale`) in `apps/macos/LocalFlow/Core/Rewrite/RewriteAttempt.swift` and extend `apps/macos/LocalFlow/Core/Storage/TranscriptionEntry.swift` with `rewriteState`, `deliveredSource` and `deliveredRewriteAttemptID` that decode as `not_requested`/nil for legacy rows.
- [X] T013 Declare `RewriteAttemptStoring` (`begin`, `recordResult`, `recordFailure`, `recordCancelled`, `markStale`, `attempts(for:)`, `cancelPendingOnStartup`, `recordDelivered`) in `apps/macos/LocalFlow/Core/DictationBoundaries.swift` and implement it on the `TranscriptionStore` actor in `apps/macos/LocalFlow/Core/Storage/TranscriptionStore.swift`: `begin` is the admission step — one transaction that re-checks the attempt limit and the per-dictation in-flight rule, assigns the ordinal, reserves quota, inserts the `pending` row and mirrors `rewrite_state`, or throws (`attempt_limit`, `concurrency_limit`, `capacity_exceeded`) having written nothing; startup `pending → failed(interrupted)` fix-up in `init` next to the existing `attempting → uncertain` fix-up, `recordOutcome` extended to write `delivered_source`, `delivered_rewrite_attempt_id` and the attempt's `delivered` flag in the same transaction; make T010 pass.
- [X] T014 Extend the existing fake store in `apps/macos/LocalFlowTests/Support/BoundaryFakes.swift` with an in-memory `RewriteAttemptStoring` implementation that enforces the same ordinal, attempt-limit, per-dictation in-flight and quota rules (throwing from `begin` without writing), and records every call for assertions.

### Settings, credential, transport, latency, metrics (delivery steps 3 and 5 groundwork)

- [X] T015 [P] Write failing tests in `apps/macos/LocalFlowTests/RewriteSettingsTests.swift` for the `RewriteSettings` snapshot rules in `data-model.md`: the snapshot carries `enabled`, `mode`, `endpointOrigin`, `timeoutSeconds`, `insecureOverride` and `credentialPresent` and no credential value (assert by reflection that no stored property contains the fake secret); `isLoopback` for `localhost`, `127.0.0.1`, `127.255.255.255`, `[::1]`, and false for a LAN or overlay address; `requiresCredential = !isLoopback`; `isUnencryptedRemote` for off-loopback `http://`; `insecureOverride` is read only for the exact origin (an override stored for `http://a:8080` does not apply to `http://b:8080` or `http://a:8081`); `canSend` truth table including `enabled = false`; `refusalCategory` precedence `insecure_endpoint_blocked` → `missing_credential` → `invalid_settings` → nil; a snapshot taken with the override on stays sendable after the override is turned off in preferences, while a new snapshot is not; timeout clamps to 5…60 on read; endpoint validation (absolute `http`/`https`, host present, origin ≤ 255 bytes); credential ≤ 4,096 bytes; origin normalization (scheme, lowercase host, explicit port).
- [X] T016 [P] Implement `apps/macos/LocalFlow/Core/Rewrite/RewriteSettings.swift` (immutable snapshot struct with the fields above, a `capture(preferences:credentialStore:)` factory that reads credential presence without copying the secret, origin normalization, loopback detection, `canSend`, `refusalCategory` returning `insecure_endpoint_blocked`, `missing_credential`, `invalid_settings` or nil) and add `rewriteEnabled`, `rewriteEndpoint`, `rewriteDefaultMode`, `rewriteTimeoutSeconds` and `rewriteInsecureOverrides` (per-origin dictionary; a write for a non-`http://` or loopback origin is ignored) with the defaults and clamping from `data-model.md` to `apps/macos/LocalFlow/Features/Settings/AppPreferences.swift`; make T015 pass.
- [X] T017 [P] Declare `RewriteCredentialStoring` (`read(origin)`, `write(origin, secret)`, `remove(origin)`) in `apps/macos/LocalFlow/Core/DictationBoundaries.swift`, implement the Keychain generic-password store (service `org.localflow.LocalFlow.rewrite`, account = origin, `SecItemAdd`/`SecItemCopyMatching`/`SecItemUpdate`/`SecItemDelete`, ≤ 4,096 bytes, errors mapped to bounded codes without the secret) in `apps/macos/LocalFlow/Core/Rewrite/RewriteCredentialStore.swift`, and add an in-memory fake to `apps/macos/LocalFlowTests/Support/BoundaryFakes.swift`.
- [X] T018 [P] Implement `apps/macos/LocalFlow/Core/Rewrite/RewriteLatency.swift`: `RewriteInstants` capturing `committed`, `sent`, `firstByte`, `terminal`, `handedOff` as `ContinuousClock.Instant`, derived `duration_ms`/`first_byte_ms`/`network_ms`, input-length bucket implementing exactly the "Input-length buckets" table in `contracts/rewrite-quality.md` (whitespace-token count of the input: `short` 1…25, `ordinary` 26…90, `long` > 90), and a report grouper that prints median and p95 per bucket and per `backend_model + prompt_version + shield_version`, prints "unmeasured" under 5 samples, and evaluates the SC-011 verdicts (`short` gate ≤ 1.5 s PASS/FAIL, `short` optimization target ≤ 1.0 s ACHIEVED/NOT ACHIEVED, `ordinary` gate p95 ≤ 3.0 s PASS/FAIL); unit-test the spans, the bucket boundaries at 25/26 and 90/91 words, the unmeasured rule and the verdicts (including median 1.18 s → PASS / NOT ACHIEVED) in `apps/macos/LocalFlowTests/RewriteClientTests.swift`.
- [X] T019 [P] Implement `apps/macos/LocalFlow/Core/Rewrite/RewriteDeliveryPolicy.swift` with `waitThenInsert` (shipped) and `insertThenReplace` (declared; `validateSelectable()` throws a startup precondition naming the four revisit steps from `research.md`), and a test in `apps/macos/LocalFlowTests/RewriteCoordinatorTests.swift` that selecting `insertThenReplace` is refused.
- [X] T020 [P] Add the content-free rewrite `Metric` cases (`rewriteTotalDuration`, `rewriteFirstByteDuration`, `rewriteNetworkDuration`, `rewriteBackendFirstTokenDuration`, `rewriteBackendDuration`, `rewriteRequestBytes`, `rewriteResponseBytes`, `rewriteInputScalars`, `rewriteAttemptOrdinal`) plus outcome, fallback and shield-failure counters with bucket and identity dimensions, and a `rewritePreAdmissionRefusal` counter keyed by reason and bucket only (no spans, no identity) to `apps/macos/LocalFlow/Core/Observability/ResourceRecorder.swift`, keep `ProcessingMetrics` untouched, and extend `apps/macos/LocalFlowTests/ResourceRecorderTests.swift` to assert the report groups by identity, prints "unmeasured" below 5 samples and lists refusal counts per reason.
- [X] T021 Write failing tests in `apps/macos/LocalFlowTests/RewriteClientTests.swift` against an in-process `URLProtocol` stub: request body contains exactly the six permitted fields and the bearer header only when a credential exists; NDJSON lines are read incrementally and the read stops at the response cap with `oversized_response` before parsing; a line over 8,192 bytes (other than `result`) is `malformed_response`; per-request timeout equals the configured timeout and yields `timed_out` no later than the configured timeout + 500 ms on a fake clock (SC-004; the tolerance is client-side handling only); task cancellation stops reading and the stream ends; a non-200 body is read to at most 8,192 bytes and only its `code` is used; `firstByte` is captured on the first received byte; health maps 200/401/403/404/non-JSON/`service` mismatch/`protocol_versions` without 1/`backend.state != ready`/503 to the eight categories in the order given in `contracts/rewrite-protocol.md`; the session is ephemeral (no cache, cookies or credential storage) with `waitsForConnectivity = false`.
- [X] T022 Declare `RewriteTransporting` (`rewrite(request:credential:timeout:) -> AsyncThrowingStream<RewriteEvent, Error>`, `health(endpoint:credential:) async throws -> HealthResponse`) in `apps/macos/LocalFlow/Core/DictationBoundaries.swift` and implement `apps/macos/LocalFlow/Core/Rewrite/RewriteClient.swift` with a lazily created ephemeral `URLSession` (keep-alive on; invalidated by the coordinator when a snapshot with `enabled = false` is observed and no attempt is pending, recreated lazily on the next request), the credential read from `RewriteCredentialStoring` at request-build time (never from the snapshot), `URLSession.bytes(for:)` line reader with byte counting before `JSONDecoder`, connection test with a fixed 10 s timeout, and request/response byte counts; make T021 pass.
- [X] T023 Add `FakeRewriteTransport` to `apps/macos/LocalFlowTests/Support/BoundaryFakes.swift`, scripted per `request_id`: succeed with a given result, delay by a fake-clock duration, fail with any category or HTTP status, deliver a stale result after a signal, emit malformed or oversized bytes, hang until cancelled, and a `failOnAnyCall` mode that fails the test if invoked; record every request for assertions.

**Checkpoint**: Protocol, storage, settings, credential, transport and latency capture exist with tests. `make check` passes and no production flow has changed.

## Phase 3: User Story 1 - Turn rewriting on or off and keep dictating either way (P1) 🎯 MVP

**Goal**: A global switch; with it off the Feature 002 flow is unchanged; with it on the faithful transcript is saved first, sent, and either the rewritten text or the faithful transcript is inserted through `insertOnce`, with a notice on failure.
**Independent test**: Run the Feature 001/002 coordinator suites with the rewrite dependency nil and with `failOnAnyCall`; then dictate with a succeeding fake and with an unreachable fake and confirm the faithful transcript is saved and available in both cases (spec US1 scenarios 1–4).

### Tests

- [X] T024 [P] [US1] Write failing tests in `apps/macos/LocalFlowTests/RewriteCoordinatorTests.swift` for `RewriteCoordinator.rewrite(dictation:text:mode:settings:)`: the admission sequence from `data-model.md` runs in order and `store.begin` is called only after steps 1–5 pass; a pending row exists before the transport is called; success records the result and returns `.rewritten(text, attempt)`; each post-admission failure category from the transport records `failed` with that category and returns `.fallback(faithful, category)`; the timeout path records `timed_out` within timeout + 500 ms on the fake clock; every pre-admission refusal — `enabled = false`, `exact`, `input_too_large` (20,001 scalars or 65,537 bytes), `missing_credential`, `insecure_endpoint_blocked`, `invalid_settings`, and `attempt_limit`/`concurrency_limit`/`capacity_exceeded` thrown by `begin` — returns `.refused(reason)` with zero `begin` rows, zero transport calls, zero ordinals consumed, `rewrite_state` unchanged and one `rewritePreAdmissionRefusal` metric; the five instants and byte counts reach `ResourceRecorder` for admitted attempts only; log capture contains no transcript text or credential.
- [X] T025 [P] [US1] Write failing tests in `apps/macos/LocalFlowTests/DictationCoordinatorTests.swift`: with `rewriter == nil` (test-only configuration) the session states, store calls and insertion calls are identical to the existing expectations; with the coordinator wired and `rewriteEnabled = false` in preferences the same holds and a `failOnAnyCall` transport is never invoked; with rewriting enabled and a succeeding fake the session passes `persisting → rewriting → inserting`, `store.commit` precedes `begin` which precedes the transport call, `insertOnce` receives the rewritten text and `recordOutcome` receives `delivered_source = rewrite` with the attempt id; with an unreachable fake `insertOnce` receives the faithful text, `delivered_source = faithful`, and the indicator notice is "Rewrite server unreachable. Original text inserted." with Retry; a pre-admission refusal during a live dictation inserts the faithful text immediately with the reason's notice and leaves no attempt row; when the captured target is gone the existing `recovery` state applies and both texts are in the store; incomplete, duration-limited, uncertain or blank results never enter `rewriting`; **no-relaunch toggle**: with the same coordinator instance, a dictation with `rewriteEnabled = false` makes no call, flipping the preference to `true` makes the next dictation call the transport, flipping it back stops the one after; **snapshot isolation**: flipping the preference, the mode, the timeout or the override while an attempt is pending changes nothing about that attempt (assert the request the fake received and the terminal record).
- [ ] T026 [P] [US1] Add a SC-001 guard to `apps/macos/LocalFlowTests/DictationCoordinatorTests.swift`, `apps/macos/LocalFlowTests/ExplicitInsertionTests.swift` and `apps/macos/LocalFlowTests/HistoryViewModelTests.swift`: a shared helper builds `AppServices`-equivalent fixtures with the coordinator always wired, `rewriteEnabled = false` and a `failOnAnyCall` transport, and re-runs the existing suites unchanged; the same helper is reused with `rewriteEnabled = true` plus Exact mode and plus a bypassed session to prove zero transport calls there too. This guard is the deterministic SC-001 gate; it proves zero `RewriteTransporting` invocations, not zero OS sockets.

### Implementation

- [X] T027 [US1] Implement `apps/macos/LocalFlow/Features/Rewrite/RewriteCoordinator.swift` (`@MainActor @Observable`): `RewriteSettings.capture` per call, the admission sequence in the order listed under "Admission" in `data-model.md` (pure checks 1–5, then `store.begin` as the only persisting step), `.refused(reason)` results that emit one refusal metric and touch nothing else, transport call with the snapshot's timeout, `RewriteResultValidator`, terminal-state recording, `RewriteInstants` capture, metric emission, and a `pendingAttempts` map bounded to 2 overall and 1 per dictation; make T024 pass.
- [X] T028 [US1] Declare `RewriteRequesting` in `apps/macos/LocalFlow/Core/DictationBoundaries.swift`, add `case rewriting` to `apps/macos/LocalFlow/Features/Dictation/DictationSession.swift`, and extend `apps/macos/LocalFlow/Features/Dictation/DictationCoordinator.swift` with an optional `any RewriteRequesting` dependency: after `store.commit` and `lifecycle.finish(lease)`, ask the coordinator to admit; on `.refused` insert the faithful text at once with the reason's notice; on admission transition to `rewriting`, await the coordinator, then insert the rewritten or faithful text through the existing `insertOnce` call with the session's captured target and record `delivered_source`/`delivered_rewrite_attempt_id`; when the dependency is nil (tests only) the code path is the existing one; make T025 and T026 pass.
- [X] T029 [US1] Wire one `RewriteCoordinator`, `RewriteClient` and the Keychain store into `apps/macos/LocalFlow/App/AppServices.swift` **unconditionally** — the coordinator is always passed to `DictationCoordinator`, and enabled state is read from the per-attempt settings snapshot, never at construction, so the Settings toggle applies to the next dictation without relaunch — and call `cancelPendingOnStartup` during store initialization in `apps/macos/LocalFlow/App/LocalFlowApp.swift`.
- [X] T030 [US1] Generalize the indicator notice in `apps/macos/LocalFlow/Features/Dictation/IndicatorPanel.swift` and `apps/macos/LocalFlow/Features/Dictation/DictationIndicator.swift` to an action notice with a message and one action button; show "Rewriting…" during `.rewriting`, the category notice with Retry after a fallback, add the `rewriting` announcement "Rewriting text. Microphone off." to `IndicatorPanel.announcement`, and announce the failure notice; extend `apps/macos/LocalFlowTests/NativePresentationTests.swift` for the new state and notice.
- [X] T031 [US1] Add the "Enable rewriting" toggle to `apps/macos/LocalFlow/Features/Settings/SettingsView.swift` and `apps/macos/LocalFlow/Features/Settings/SettingsViewModel.swift`, disabled with an inline reason while a fresh `RewriteSettings` snapshot (ignoring `enabled`) would refuse (full Settings UI arrives in US7), and add the toggle-gating test to `apps/macos/LocalFlowTests/SettingsTests.swift`.

**Checkpoint**: Rewriting can be switched on against a fake or a real server without relaunch; every post-admission failure inserts the faithful transcript with a notice; pre-admission refusals insert it with no row; the Feature 001/002 suites pass with the dependency nil and with the wired coordinator on a transport that fails on any call (SC-001, SC-002 for success/failure/fallback).

## Phase 4: User Story 2 - Choose how much the text is rewritten (P1)

**Goal**: Exact, Clean, Polished and Concise are distinct selectable modes with a configurable default; Exact makes no request; the selected mode is transmitted and recorded.
**Independent test**: Send the same faithful transcript in each mode against the fake and confirm the request carries the mode and the attempt row stores it; Exact produces no request (spec US2 scenarios 1–2). Scenarios 3–4 are reviewed in the quality corpus (Phase 5 and Phase 11).

### Tests

- [X] T032 [P] [US2] Add tests to `apps/macos/LocalFlowTests/RewriteCoordinatorTests.swift`: Exact is a pre-admission eligibility outcome (no attempt row, no transport call, no notice, no refusal metric); Clean, Polished and Concise each appear as `mode` in the request body and in the stored attempt; the mode is taken from the snapshot captured at admission, so changing the default mode while an attempt is pending does not change its request; a result whose `mode` differs from the request is `malformed_response`.
- [X] T033 [P] [US2] Add tests to `apps/macos/LocalFlowTests/AppPreferencesTests.swift`: `rewriteDefaultMode` defaults to `clean`, persists each value, and unknown stored strings read as `clean`.

### Implementation

- [X] T034 [US2] Pass the effective mode (session override if any, otherwise `rewriteDefaultMode`) from `apps/macos/LocalFlow/Features/Dictation/DictationCoordinator.swift` into the rewrite request and treat `exact` as "not requested" with no attempt row; make T032 pass.
- [X] T035 [US2] Add the "Default mode" picker (Exact/Clean/Polished/Concise with the one-line mode definitions from FR-002) to `apps/macos/LocalFlow/Features/Settings/SettingsView.swift` and `SettingsViewModel.swift`; make T033 pass.

**Checkpoint**: Mode selection is transmitted and recorded; Exact is a no-op.

## Phase 5: User Story 3 - Slovak, English and mixed technical language stay in the speaker's language (P1)

**Goal**: The request never asks for translation, `language_hints` is present and empty in client v1, and a deterministic corpus, protected-entity checker and semantic mutation detectors exist so language and terminology preservation can be measured (FR-005, FR-023, SC-005, SC-006).
**Independent test**: `python3 scripts/test-rewrite-quality.py` passes offline: verbatim preservation accepted, each mutation class rejected or flagged, NFC and repeated values handled, unmeasured buckets reported, missing identity block refused. The live corpus run and review are Phase 11.

### Tests

- [X] T036 [P] [US3] Add a test to `apps/macos/LocalFlowTests/RewriteProtocolTests.swift` that the encoded request has `language_hints: []` and no translation-related field, and that a request with more than 4 hints is refused before sending.
- [X] T037 [P] [US3] Write `scripts/test-rewrite-quality.py` (runs offline in `make check`) with fixed input/output pairs per `contracts/rewrite-quality.md`: protected check accepts verbatim preservation and rejects dropped, altered, count-changed and identifier case-changed values, handles NFC and repeated values; each semantic detector flags its mutation fixture (negation dropped, negation added, owner swapped, day shifted, number changed, number spelled out, commitment dropped, sentence translated, diacritics stripped) and passes its clean fixture; bucketing follows the "Input-length buckets" table in `contracts/rewrite-quality.md` (assert 25/26 and 90/91 boundaries); the SC-011 verdict fields `short_gate_pass` (≤ 1.5 s), `short_target_achieved` (≤ 1.0 s) and `ordinary_gate_pass` (p95 ≤ 3.0 s) are computed correctly (a 1.18 s short median gives `true`/`false`) and are `null` under 5 samples; summary reports "unmeasured" under 5 samples; a result set without a full identity block is refused; corpus coverage minimums (40 items, ≥ 8 per language, one item per protected class and fact type, ≥ 5 `short`, ≥ 5 `ordinary`, ≥ 1 `long`) are asserted against `fixtures/rewrite/corpus-v1.json`.

### Implementation

- [X] T038 [US3] Author `fixtures/rewrite/corpus-v1.json` (`corpus_version: 1`) with at least 40 public, non-private items: ≥ 8 English, ≥ 8 Slovak, ≥ 8 mixed Slovak/English technical; at least one item each with names, IP addresses, explicit numbers, currency, dates, times, URLs, email addresses, filesystem paths, version strings, negation, task ownership, two commitments in one text, at least 5 `short` items, at least 5 `ordinary` items and at least one `long` item per the bucket table in `contracts/rewrite-quality.md`; every item annotates `protected` by class, `facts` by type and per-mode `review` expectations; include the two spec examples (Peter/Odoo/Monday in Polished; Proxmox/VM/NetBird mixed).
- [X] T039 [US3] Implement the shared checker module used by both scripts in `scripts/rewrite_quality_lib.py`: protected-entity verbatim check with NFC normalization and occurrence counting (identifiers case-sensitive), the six semantic detectors from `contracts/rewrite-quality.md` (negation marker parity for `en`/`sk`, owner-and-action co-occurrence with swap detection, day/month-name set equality across both languages and cases, quantity presence after numeral normalization, commitment-marker sentence count, language-mix script/stop-word profile with the 20% diacritics tolerance), input-length bucketing per the single bucket table, median/p95 with the unmeasured rule, the SC-011 verdict fields, and identity-block validation; make T037 pass.
- [X] T040 [US3] Implement `scripts/rewrite-quality.py <corpus> <output-dir> --endpoint URL [--modes] [--credential-env NAME] [--continue]` (development-only, not in `make check`): refuse to start unless health carries every identity field, send one protocol request per item and mode with a fresh `request_id` holding at most one response in memory, validate the `result` with the same rules as the client, run the checker and detectors, run the shield detector regexes over annotated values and report detector misses separately, and write `results.json`, `summary.json` (with the three SC-011 verdict fields) and `review-template.md` exactly as specified in `contracts/rewrite-quality.md`; never print transcript text or the credential.
- [X] T041 [US3] Add `python3 scripts/test-rewrite-quality.py` to `scripts/test.sh` and verify `make check` remains offline.

**Checkpoint**: The quality tooling is deterministic and offline; the corpus is committed; nothing is claimed about model quality yet.

## Phase 6: User Story 4 - Retry or cancel without recording again (P2)

**Goal**: Retry in any mode from the completed state or history reusing the stored snapshot; cancel a pending rewrite; stale, late and mismatched responses are recorded and never applied; attempt and in-flight limits are enforced by immediate refusal.
**Independent test**: With fakes that delay, fail, then succeed, retry several times, cancel mid-flight, deliver a stale response after a newer attempt, and hit the caps; confirm attempt ordering, current-result selection, no recognition call and control return within one run-loop turn (spec US4 scenarios 1–4, SC-003, SC-004).

### Tests

- [X] T042 [P] [US4] Add tests to `apps/macos/LocalFlowTests/RewriteCoordinatorTests.swift`: retry after failure creates ordinal 2 with the same `input_text` and hash and calls no recognition or audio boundary; retry in a different mode records that mode; cancel records `cancelled` immediately, cancels the transport task, and a result arriving afterwards sets `stale = 1` without changing state; a response for an older attempt arriving after a newer one succeeded is marked stale and the newer stays current; a result for a different `request_id` is `request_mismatch`; the eleventh retry is refused with `attempt_limit`, the limit explanation, and no row; a second request for the same dictation while one is pending is refused with `concurrency_limit` and no row; the third overall in-flight request, from a live dictation and from history alike, returns `.refused(concurrency_limit)` immediately with no row, no ordinal and no transport call, and leaves the two pending attempts untouched (the live flow then inserts the faithful text; the history flow changes nothing); a history-initiated attempt never calls `insertOnce` on success or failure and its terminal state is visible through `attempts(for:)`; the stale check runs after every await (assert with a fake transport that completes the older attempt between awaits).
- [X] T043 [P] [US4] Add tests to `apps/macos/LocalFlowTests/DictationCoordinatorTests.swift`: Escape or the indicator action during `rewriting` cancels only the rewrite, the faithful text is inserted and the notice reads "Rewrite cancelled. Original text inserted."; starting a new dictation while another's attempt is pending never cancels it and its late result completes into history without insertion; 20 sequential dictations with mixed scripted outcomes and 5 overlapping dictations never associate a result with the wrong dictation or attempt; the concurrency-cap notice is "Two rewrites are still running. Original text inserted." with Retry and the refused dictation has zero attempt rows and `rewrite_state = not_requested`.
- [X] T044 [P] [US4] Add a timing test to `apps/macos/LocalFlowTests/RewriteCoordinatorTests.swift` that `cancel` returns control before the next run-loop turn and records the attempt as cancelled within the same turn (SC-004).

### Implementation

- [X] T045 [US4] Extend `apps/macos/LocalFlow/Features/Rewrite/RewriteCoordinator.swift` with `retry(dictation:mode:origin:)` (uses the first attempt's `input_text` or `transcriptions.text`, runs the same admission sequence with a fresh snapshot, next ordinal only on admission; `origin: .history` attempts complete into the store without any insertion or fallback), `cancel(dictation:)` (records `cancelled` first, then cancels the transport task), the newest-and-pending staleness check after every await and before persistence or insertion, per-dictation and global in-flight refusal with `concurrency_limit` before `begin`, the `attempt_limit` refusal, and a `Retry` action on the notice; make T042 and T044 pass.
- [X] T046 [US4] In `apps/macos/LocalFlow/Features/Dictation/DictationCoordinator.swift` and `apps/macos/LocalFlow/Features/Dictation/ControlMailbox.swift`, route Escape and the indicator action during `.rewriting` to `RewriteCoordinator.cancel`, keep other dictations' pending attempts independent of the session, and record `delivered_source` for the fallback; make T043 pass.

**Checkpoint**: Retry, cancel, staleness and caps behave deterministically with no recognition re-run; every refusal is pre-admission and leaves no row (FR-007, FR-007a, FR-008, FR-009, FR-011).

## Phase 7: User Story 5 - Inspect the faithful transcript and every rewrite separately (P2)

**Goal**: History shows a rewrite badge; detail shows Feature 002 sections unchanged plus a Rewrite section with the AI-generated label, delivered-versus-current line, the ordered attempts list, Cancel, Retry/Rewrite with a mode picker, Copy and explicit Insert of either text; deletion removes attempts; legacy rows read "not requested".
**Independent test**: Complete a dictation with two attempts, restart (reopen the store), inspect the entry, insert a chosen attempt explicitly and confirm the delivered record, then delete and confirm zero attempt rows (spec US5 scenarios 1–5, SC-008).

### Tests

- [X] T047 [P] [US5] Write failing tests in `apps/macos/LocalFlowTests/RewriteHistoryTests.swift` and `apps/macos/LocalFlowTests/HistoryViewModelTests.swift`: list rows expose a badge from `rewriteState` and none for `not_requested`; detail loads attempts ordered by ordinal with mode, state, duration, first-byte, failure category, stale, delivered and identity fields; the current rewrite is the highest-ordinal succeeded attempt; the "Delivered" line names faithful or "rewrite attempt N (Mode)" and flags when current differs from delivered; after a simulated restart (`TranscriptionStore` reopened on the same file) all attempts and snapshots are intact and a previously pending attempt reads `failed/interrupted`; deletion leaves zero attempt rows; a pre-003 fixture database shows "Rewrite: not requested" and no attempts; the attempt-limit explanation appears at ten attempts; a Retry from the detail view with a succeeding fake updates the attempts list and never calls `insertOnce` or `ExplicitInsertionCoordinator`.
- [X] T048 [P] [US5] Add tests to `apps/macos/LocalFlowTests/ExplicitInsertionTests.swift`: explicit insertion of a chosen rewrite attempt's text runs the existing target selection and confirmation flow, and on success writes `delivered_source = rewrite`, `delivered_rewrite_attempt_id` and the attempt's `delivered` flag; explicit insertion of the faithful text writes `delivered_source = faithful` and clears the attempt reference.

### Implementation

- [X] T049 [US5] Extend `apps/macos/LocalFlow/Features/Transcriptions/HistoryViewModel.swift` with `rewriteState` badges, `attempts(for:)` loading through `RewriteAttemptStoring`, current/delivered derivation, and Retry/Rewrite/Cancel actions calling `RewriteCoordinator` with `origin: .history` (results land in the store and refresh the detail; nothing is inserted; a pre-admission refusal shows its history notice without "Original text inserted."); make the view-model parts of T047 pass.
- [X] T050 [US5] Add the badge to `apps/macos/LocalFlow/Features/Transcriptions/HistoryView.swift` and the "Rewrite" section to `apps/macos/LocalFlow/Features/Transcriptions/TranscriptionDetailView.swift` after the unchanged Feature 002 sections: saved text beside the current rewrite labelled "AI-generated rewrite, not the transcript", Copy and Insert for each (the saved text's Insert is the FR-021 "use faithful transcript instead" action for history and recovery; no separate control is added), the Delivered line, the attempts list, Cancel for a pending attempt, Retry/Rewrite with a mode picker, and the attempt-limit explanation; with accessibility labels.
- [X] T051 [US5] Extend `apps/macos/LocalFlow/Features/Transcriptions/ExplicitInsertionCoordinator.swift` to insert a chosen text (faithful or a specific attempt) through its existing target selection and confirmation flow and to record the delivered source and attempt via `recordOutcome`; make T048 pass.

**Checkpoint**: Every attempt is inspectable after restart, deletion cascades, legacy history is untouched (FR-014, FR-015, FR-021, SC-008).

## Phase 8: User Story 6 - Skip rewriting for one dictation (P2)

**Goal**: Holding Shift while releasing the push-to-talk shortcut marks the session local-only: no request, state `not_requested`, faithful transcript inserted; the dictation can be rewritten later from history.
**Independent test**: With rewriting enabled, release with Shift held and confirm no transport call, `not_requested`, faithful insertion; then request Rewrite from history and confirm a new attempt with no recording (spec US6 scenarios 1–2).

### Tests

- [X] T052 [P] [US6] Add tests to `apps/macos/LocalFlowTests/ShortcutControllerTests.swift` that a release with Shift held reports `bypassRewrite = true`, that the flag is false when Shift is part of the configured shortcut, and that the press path is unchanged.
- [X] T053 [P] [US6] Add tests to `apps/macos/LocalFlowTests/DictationCoordinatorTests.swift`: a bypassed session creates no attempt row, calls no transport (`failOnAnyCall`), emits no refusal metric, inserts the faithful text and records `not_requested`; a later `RewriteCoordinator.retry(origin: .history)` on that dictation creates ordinal 1 with `transcriptions.text` as the snapshot and inserts nothing.

### Implementation

- [X] T054 [US6] Read the Shift modifier at release in `apps/macos/LocalFlow/Features/Dictation/ShortcutController.swift`, expose it on the stop event, and suppress the gesture when Shift is in the configured shortcut; make T052 pass.
- [X] T055 [US6] Carry the bypass flag on the session in `apps/macos/LocalFlow/Features/Dictation/DictationSession.swift` and treat a bypassed session as an eligibility outcome in `apps/macos/LocalFlow/Features/Dictation/DictationCoordinator.swift` (no admission, no row, no notice); make T053 pass. The Settings note for the gesture is built in T059, not here.

**Checkpoint**: A single dictation can stay on the Mac while rewriting remains globally on (FR-012).

## Phase 9: User Story 7 - Configure and test the rewrite server (P2)

**Goal**: Settings for endpoint, enabled state, default mode, timeout, credential and the per-origin insecure transport override per FR-016a (HTTPS default; loopback HTTP free; non-loopback HTTP blocked until the override is on, then credential mandatory plus persistent warning; revoking the override disables the endpoint at once); a connection test that distinguishes eight categories and shows the server's identity; the credential lives only in Keychain; every change reaches the next dictation without relaunch.
**Independent test**: Point Settings at fakes for each category and confirm the displayed status; confirm `defaults` holds no secret and the Keychain item exists; confirm the override resets when the origin changes and that revoking it turns rewriting off (spec US7 scenarios 1–5, SC-010).

### Tests

- [X] T056 [P] [US7] Write failing tests in `apps/macos/LocalFlowTests/RewriteSettingsTests.swift` and `apps/macos/LocalFlowTests/SettingsTests.swift` for `SettingsViewModel`: endpoint validation messages; `https://` non-loopback needs only a credential, loopback `http://` needs nothing, non-loopback `http://` needs the override and a credential; the enable toggle refuses with an inline reason while the endpoint is invalid, a required credential is missing, or the origin is off-loopback `http://` without the override; the override control appears only for an off-loopback `http://` origin, is stored per exact origin, is not applied to any other origin, and clears when the origin changes; turning the override off while rewriting is enabled sets `rewriteEnabled = false` immediately and shows why; the persistent warning is present while the override is on and its text names the host and states that authentication does not encrypt transcripts; changing the origin re-reads the credential for the new origin; the credential never appears in `UserDefaults`, in the `RewriteSettings` snapshot, in the view-model's diagnostics export or in log capture; the masked field supports Set, Reveal and Remove; the timeout stepper clamps to 5…60; a change to any control is visible in the next `RewriteSettings.capture` without recreating the view model or coordinator.
- [X] T057 [P] [US7] Add connection-test tests to `apps/macos/LocalFlowTests/SettingsTests.swift` using `FakeRewriteTransport`: each of the eight categories (`connected` with server name/version, backend kind/model, prompt versions, shield version and protocol version displayed; `authenticationFailed`; `serverUnreachable`; `rewriteServiceUnavailable`; `backendUnavailable`; `incompatibleVersion`; `missingCredential` and `insecureEndpointBlocked` without any transport call) maps to its plain-language status string; the raw HTTP body never reaches the UI; the test uses the 10 s timeout.

### Implementation

- [X] T058 [US7] Extend `apps/macos/LocalFlow/Features/Settings/SettingsViewModel.swift` with the rewrite section state (endpoint, enabled, default mode, timeout, credential presence, override, warning, inline refusal reason, connection-test result), the Keychain store calls, the origin-change handling, and `testConnection()` through `RewriteTransporting.health`; make T056 and T057 pass.
- [X] T059 [US7] Build the rewriting section in `apps/macos/LocalFlow/Features/Settings/SettingsView.swift`: Endpoint field, Enable toggle with inline reason, Default mode picker, Timeout stepper, masked Credential field with Set/Reveal/Remove, "Allow unencrypted connection to this server (insecure)" toggle shown only for off-loopback `http://`, the persistent warning above the controls while on ("Transcripts and the credential travel unencrypted to <host>. Authentication does not encrypt them. Use this only on a network you trust."), Test connection button with result line and identity details, and the single authoritative "Hold Shift on release to skip rewriting once" note (or the unavailable-because-Shift-is-in-the-shortcut note, per T054's flag); all controls labelled for accessibility and the warning announced when it appears.
- [X] T060 [US7] Add `RewriteConnectionTestResult` (category, protocol versions, identity fields, `testedAt`, bounded content-free `diagnostic`) to `apps/macos/LocalFlow/Core/Rewrite/RewriteProtocol.swift` and the developer-diagnostics code path (bounded codes in the `DictationErrorMessage` style, never the body) so raw errors are available only there.

**Checkpoint**: Settings, credential storage, the transport policy and the connection test are complete (FR-016, FR-016a, FR-017, SC-010 eight categories).

## Phase 10: Reference server (`flowd rewrite`)

**Purpose**: The thin Go endpoints and one streaming OpenAI-compatible adapter so the feature can be run and measured against the owner's MTPLX host (initial model `mtplx-qwen35-9b-optimized-speed`). No story label: this phase serves US1, US2, US3 and US7 together and is required for every live acceptance task in Phase 11. Constitution principle 8: no weights, standard library only, separate inference process.

### Tests

- [X] T061 [P] Write failing handler tests in `server/internal/rewrite/handler_test.go` with `httptest`: health returns the payload shape from `contracts/rewrite-protocol.md` with cached backend probe (at most one probe per 5 s), `backend.state` `ready`/`loading`/`unavailable`, `shield_version` 0 under `--shield=off`; POST rejects bodies over 262,144 bytes with 413 before decoding; unknown fields, wrong version, bad UUID and bad mode give 400 with the right code; missing or wrong bearer gives 401/403 when a token is configured; the third concurrent request gets 429 `server_busy` with no queueing; a successful run emits optional `accepted`, `progress` at most every 250 ms, exactly one `result` with identity and timing fields, and no `delta` when `stream_deltas` is false; closing the client connection cancels the backend stream; a runaway backend (fake streams past `min(4 × input_bytes, 65,536)`) produces exactly one `error` event with code `output_too_large`, no `result`, and the fake observes its connection closed within one fragment of the bound; logs contain request ids, sizes, timings and codes but never `text`, prompts or placeholder values, including on the runaway path.
- [X] T062 [P] Write failing adapter tests in `server/internal/rewrite/backend/openai_test.go` against an in-process fake OpenAI-compatible SSE server in `server/internal/rewrite/backend/fake.go`: streamed completion is always requested; `backend_first_token_ms` is recorded; a first-token timeout (default 5 s) yields `backend_first_token_timeout`; a total backend timeout yields `backend_timeout`; connection refused yields `backend_unavailable`; a JSON schema/grammar constraint is sent when the backend advertises support and omitted otherwise; the reported model id is bounded to 128 bytes; placeholder glyphs round-trip through the fake backend; `--debug-delay` delays the response for acceptance doubles; **bounded accumulation**: with a `fake.Runaway(fragmentBytes)` mode that streams indefinitely, the adapter returns `ErrOutputTooLarge` and cancels the backend request as soon as `accumulated + fragment > maxOutputBytes` (assert the fake's handler sees the context cancelled and that the number of fragments it wrote is ≤ ceil(maxOutputBytes / fragmentBytes) + 1), the returned partial text is empty, and a heap-allocation measurement over the accumulation buffer (`testing.AllocsPerRun` or a capacity assertion on the builder) shows it never grows past `maxOutputBytes`; a single SSE line over 65,536 bytes is rejected without being buffered; a backend error body is read to at most 8,192 bytes.
- [X] T063 [P] Write failing shield tests in `server/internal/rewrite/shield/shield_test.go`: each detector class (IP, URL, email, filesystem path, version string, currency, explicit number, date, time) matches fixed positive cases and ignores fixed negatives; substitution produces `⟦E<n>⟧` placeholders in order; restoration succeeds only when every placeholder appears exactly once and no unknown placeholder appears, else `shield_restore_failed`; every annotated non-identifier value in `fixtures/rewrite/corpus-v1.json` is matched by the detector set (a miss is a detector defect).

### Implementation

- [X] T064 [P] Implement `server/internal/rewrite/shield/shield.go`: `shield_version: 1` detector set with committed regexes, `Shield(text) (shielded string, table)`, `Restore(output, table) (string, error)`, placeholder and restored counts; make T063 pass.
- [X] T065 [P] Implement `server/internal/rewrite/prompts/prompts.go` with versioned per-mode templates (`clean`, `polished`, `concise`, each `prompt_version: 1`) that instruct the model to return only the rewritten text, keep placeholders verbatim, preserve language mix, never translate, summarize or invent, plus the JSON schema used for constrained output; add a test in `server/internal/rewrite/prompts/prompts_test.go` that the version changes whenever a template's text hash changes.
- [X] T066 Implement `server/internal/rewrite/backend/openai.go`: streaming `/v1/chat/completions` adapter with first-token and total timeouts, a `maxOutputBytes` parameter (`min(4 × input_bytes, 65,536)`) checked before appending each streamed fragment — on overflow cancel the backend request immediately, discard the partial buffer and return `ErrOutputTooLarge` — a per-line SSE cap of 65,536 bytes, an 8,192-byte cap on backend error bodies, progress callback, model-id probe for health, cancellation on context done, and `--debug-delay` support; make T062 pass.
- [X] T067 Implement `server/internal/rewrite/handler.go` and `server/internal/rewrite/limits.go`: health with cached probe and identity fields, POST with bearer check, size gate, request validation, concurrency limit 2, shield → prompt → adapter (with `maxOutputBytes` from the request) → restore → server-side result validation (non-empty, within the same size bound after restoration, no leading commentary, placeholders restored) → NDJSON `result` or `error` (`ErrOutputTooLarge` → `output_too_large`, counted in a content-free metric), `timing` with `queue_ms`, `backend_first_token_ms`, `backend_ms`, and content-free logging; make T061 pass.
- [X] T068 Add the `rewrite` subcommand with `--listen`, `--backend`, `--model`, `--shield=on|off`, `--debug-delay`, `--protocol-versions`, `--first-token-timeout`, `--backend-timeout` and `LOCALFLOW_REWRITE_TOKEN` to `server/cmd/flowd/main.go`; document it in `server/README.md` and `docs/architecture/server.md`; keep `go test ./...`, `go vet ./...` and `gofmt` clean under `make check`.

**Checkpoint**: `go run ./server/cmd/flowd rewrite` serves health and rewrite against MTPLX per `quickstart.md`; the acceptance doubles (`--debug-delay`, `--protocol-versions 2`, `--shield=off`, stopped backend) behave as described.

## Phase 11: Quality, acceptance and polish

**Purpose**: Turn the design targets into measured evidence, write the two ADRs, and close cross-cutting items. Hardware, latency, quality and privacy results are recorded from real runs only; unavailable evidence stays explicitly unverified.

- [X] T069 [P] FR-022 traceability audit, not a new test pass: produce `specs/003-server-rewriting/acceptance/fr-022-traceability.md` mapping each FR-022 scenario (successful rewrite, malformed, wrong schema version, empty, oversized, timeout, cancellation, stale response, authentication failure, server unavailable, retry, faithful fallback, deletion, multiple sequential and concurrent dictations) to exactly one named existing test from T007/T010/T021/T024/T025/T032/T042/T043/T047 — deletion maps to the storage tests, not the coordinator — and, only where a scenario has no test that also asserts "faithful transcript present and unchanged afterwards; no invalid, stale, cancelled or mismatched text inserted or shown as current" (SC-002), add that assertion to the existing test or add one named test in the file that owns the behavior; rename tests where needed so each name contains its scenario; confirm determinism with the local loop `for i in 1 2 3; do make check || exit 1; done` (no CI exists in this repository; if one is added by implementation time, run it there instead) and record the three results in the traceability file.
- [X] T070 [P] Add a privacy test in `apps/macos/LocalFlowTests/RewriteClientTests.swift` that runs the full fake acceptance flow with log capture and the metric export and asserts neither contains any corpus sentence from `fixtures/rewrite/corpus-v1.json` nor the fake credential (FR-018, SC-010).
- [X] T071 [P] Write `docs/adr/0013-rewrite-protocol-v1.md` (NDJSON over HTTP/1.1, single validated `result`, identity fields, size and version rules) and `docs/adr/0014-no-background-text-replacement.md` (rejection of insert-then-replace with the four revisit steps), and list both in `docs/adr/README.md`.
- [X] T072 [P] Update `docs/architecture/overview.md`, `docs/architecture/storage.md` (migration `rewrite-v4`, cascade, quota accounting) and `README.md` (rewriting is opt-in, self-hosted, Shift-bypass, connection test) to describe the shipped behavior without claiming unmeasured results.
- [ ] T073 Run the connection-test walkthrough from `quickstart.md` against the real server, a wrong secret, a closed port, `python3 -m http.server`, a stopped backend and `--protocol-versions 2`, plus the off-loopback `http://` override sequence (blocked → override on with warning → missing credential → connected → override revoked disables → origin change resets → second `http://` origin still blocked) against the owner's overlay-network address; record each of the eight observed statuses and whether `NSAllowsLocalNetworking` alone would have covered the overlay address (T003 follow-up) in `specs/003-server-rewriting/acceptance/connection-test.md` (SC-010, FR-016a).
- [ ] T074 Run dictation flows 1–9 from `quickstart.md` in the signed installed app against the reference server, including kill-during-rewriting and relaunch, and record the observed states, notices and `sqlite3` counts in `specs/003-server-rewriting/acceptance/dictation-flows.md` (SC-007, SC-008).
- [X] T075 Run `scripts/rewrite-quality.py` on the reference setup with shielding on and again with `--shield=off`, keep outputs under a 0700 `build/rewrite-quality-<date>/`, and record the identity block, per-mode protected-entity pass rates (SC-005 hard gate), shield placeholder/restore counts, detector flags and any detector misses in `specs/003-server-rewriting/acceptance/quality-summary.md`; a protected-entity failure is recorded as a failed gate, never relabeled.
- [ ] T076 Complete the owner review from `review-template.md` into `specs/003-server-rewriting/acceptance/rewrite-review-<date>.md` with reviewer, date, corpus hash, run id, identity block, per-(item, mode) verdicts with input/output hashes, per-mode pass rates, the zero-tolerance negation/ownership counts, the Slovak/mixed language-preservation verdicts for US3 and US2 scenarios 3–4, and the list of unreviewed items (SC-006).
- [ ] T077 Measure SC-011: with the model warm, run 20 `short` and 20 `ordinary` sequential dictations (buckets per `contracts/rewrite-quality.md`) through the app, export the resource report, combine with the runner's `summary.json`, and write `specs/003-server-rewriting/acceptance/latency.md` opening with the full evidence identity block (Mac hardware, macOS version, LocalFlow version/build/commit, flowd version/commit, backend, model identity/tag, prompt and shield versions, protocol version, warm/cold state and how warmed, network topology, configured timeout) and listing per bucket: the measured median/p95 per span, the explicit verdicts — `short`: `acceptance (median ≤ 1.5 s): PASS|FAIL` and `1.0 s optimization target: ACHIEVED|NOT ACHIEVED`; `ordinary`: `acceptance (p95 ≤ 3.0 s): PASS|FAIL`; `long`: reported, no gate — and the dominant span for any miss; groups under 5 samples read "unmeasured"; a failed gate is recorded as failed; the timeout is stated as the ceiling and never quoted as latency.
- [ ] T078 Run the Feature 001 20-cycle memory protocol from `docs/performance/memory-budget.md` twice (rewriting disabled; rewriting enabled with the server reachable) on the reference machine, confirm with `vmmap` that no inference runtime is mapped in the client, and record the evidence identity block, idle RSS, the max(5 MB, 5%) comparison and recording overhead in `specs/003-server-rewriting/acceptance/memory.md` (SC-009, FR-019).
- [X] T082 Measure `flowd rewrite` memory separately from the inference backend on the reference machine: idle RSS after start with the backend probe warm, RSS sampled every second during an `ordinary`-bucket request, and settled RSS 30 s after 20 sequential requests (`ps -o rss` or `footprint` on the `flowd` process only; the backend process is excluded and named as excluded); record the evidence identity block, OS, flowd build/commit and flags, request conditions, the three figures and their comparison against the 100 MB idle / 250 MB processing targets from `docs/performance/memory-budget.md` and constitution principle 8 in `specs/003-server-rewriting/acceptance/server-memory.md`; a target is "unmeasured" until this file exists and "missed" rather than relaxed if exceeded.
- [ ] T079 Run the privacy check from `quickstart.md`: search XCTest log capture and server logs from T074/T075 for corpus sentences and the credential, run `defaults read org.localflow.LocalFlow` and `security find-generic-password -s org.localflow.LocalFlow.rewrite`, and record the results in `specs/003-server-rewriting/acceptance/privacy.md` (FR-018, SC-010).
- [X] T080 Update `specs/003-server-rewriting/acceptance/baseline.md` with the SC-001 evidence at two levels, kept distinct: (1) deterministic gate — Feature 001/002 suites passing with the dependency nil and with the wired coordinator on `failOnAnyCall` (T026), proving zero `RewriteTransporting` invocations with rewriting disabled, in Exact mode and when bypassed; (2) optional live evidence — the observation from quickstart flow 1 (`nettop -p LocalFlow` or an equivalent local tool, if available) during dictation, history and Settings use with rewriting disabled, recorded as an observation, or "not observed" if no tool was used; never present (1) as proof that the OS opened zero sockets.
- [X] T081 Final `make check`, `swift format` and `.specify/scripts/bash/check-prerequisites.sh --json --require-spec` run; record the command output summary and any remaining unverified gates in `specs/003-server-rewriting/acceptance/baseline.md`.

## Dependencies and execution order

### Phase dependencies

- **Phase 1 (Setup)**: none; T002 and T003 run in parallel with T001; T004 last.
- **Phase 2 (Foundational)**: after Phase 1. Inside it: T005/T006/T007/T009 in parallel; T008 after T007; T010 before T011–T013; T014 after T013; T015–T020 in parallel after T008; T021 before T022; T023 after T008. Blocks every story phase.
- **Phase 3 (US1)**: after Phase 2. Required by every later story phase because it introduces `RewriteCoordinator`, the `rewriting` state and the notice.
- **Phase 4 (US2)** and **Phase 8 (US6)**: after Phase 3; independent of each other.
- **Phase 5 (US3)**: T036 after Phase 2; T037–T041 depend only on Phase 1 and can run any time after it, in parallel with Phases 3–9.
- **Phase 6 (US4)**: after Phase 3.
- **Phase 7 (US5)**: after Phase 6 (retry/cancel actions) .
- **Phase 9 (US7)**: after Phase 3 (T031 stub) and T022 (health); independent of Phases 4–8.
- **Phase 10 (Server)**: T061–T065 after T005/T009 and T038; T066–T068 after their tests. Runs in parallel with Phases 3–9.
- **Phase 11**: T069–T072 after Phases 3–9; T073–T079 and T082 after Phase 10 and Phases 3–9; T080–T081 last.

### User story dependencies

- **US1 (P1)**: foundation only. MVP.
- **US2 (P1)**: US1.
- **US3 (P1)**: tooling is independent; live language-preservation evidence needs US1, US2 and the server.
- **US4 (P2)**: US1.
- **US5 (P2)**: US4 for Retry/Cancel actions; storage from Phase 2.
- **US6 (P2)**: US1.
- **US7 (P2)**: US1 (toggle) and the transport's health call; otherwise independent.

### Parallel opportunities

- Phase 2: `[P]` groups T005/T006/T007/T009, T010, T015–T020.
- Story phases: tests marked `[P]` within a phase run together; Phases 4, 6, 8 and 9 can proceed in parallel after Phase 3 when staffed.
- Phase 5 tooling (T037–T041) and Phase 10 server (T061–T068) run alongside client story work.
- Phase 11: T069–T072 in parallel; the live acceptance tasks T073–T079 and T082 need the reference machine and run sequentially.

## Parallel example: User Story 1

```text
# After Phase 2, launch the US1 tests together:
Task: T024 RewriteCoordinator tests in apps/macos/LocalFlowTests/RewriteCoordinatorTests.swift
Task: T025 DictationCoordinator rewrite-flow tests in apps/macos/LocalFlowTests/DictationCoordinatorTests.swift
Task: T026 SC-001 guard in DictationCoordinatorTests, ExplicitInsertionTests, HistoryViewModelTests

# Then sequentially: T027 → T028 → T029 → T030 → T031
```

## Implementation strategy

### MVP first (User Story 1)

1. Phase 1 and Phase 2 (protocol, storage, settings, transport, latency, fakes).
2. Phase 3: switch, fallback, notice, minimal Settings toggle.
3. Stop and validate: Feature 001/002 suites with nil and `failOnAnyCall`; fake success and unreachable runs; `make check`.
4. Bring up Phase 10 in parallel so the MVP can be exercised against `llama-server`.

### Incremental delivery

1. US2 modes → Exact no-op and mode recording verified.
2. US4 retry/cancel/staleness/caps → SC-003 and SC-004 deterministic evidence.
3. US5 history detail → SC-008 restart and deletion evidence.
4. US6 bypass and US7 settings/connection test → SC-010 categories.
5. US3 tooling then Phase 11 live quality, latency, client and server memory and privacy evidence; FR-022 traceability audit; ADRs; docs.

### LocalFlow required task coverage

Lifecycle and cancellation: T024, T042–T046. Bounded overload: T007, T010, T021, T024 (pre-admission refusals), T042, T061, T062 (server bounded accumulation). Offline and recovery: T010, T013, T025, T047, T074. Runtime settings without relaunch: T015, T025, T029, T056. Local instrumentation: T018, T020, T077. Repeatable resource acceptance: T077, T078, T082. Hardware, latency, quality and privacy acceptance (T073–T079, T082) are recorded only from real runs on the reference machine, never from fakes or scaffolding builds.

## Correction-learning filtering increment (2026-09-17)

- [x] CF01 Inspect detection, vocabulary conflict/capacity rules, Undo, lifecycle and existing tests; record specification and constitution-compliant plan.
- [x] CF02 Add candidate/scoring boundary, compact suppression sets, bounded spelling similarity and protected-literal exclusions.
- [x] CF03 Add bounded digest-only repetition history; integrate assessment before dictionary mutation and preserve internal suggest disposition.
- [x] CF04 Update Settings copy and add scorer/watcher regression coverage, including capacity, duplicates, disable, Undo and passage bounds.
- [x] CF05 Run `make check` across Feature 001/002/003 and record results; converge against this increment's requirements.

Correction-filter convergence and test evidence: [acceptance/correction-filter.md](acceptance/correction-filter.md). Existing live Feature 003 acceptance tasks remain separate.

## Phase 11 acceptance disposition (2026-09-17)

The owner asked to leave owner-only checks pending. T073/T074/T076/T077/T078 and full T079 remain open; their acceptance files name the missing evidence. T075 is complete as a measurement task, with SC-005 recorded as FAIL. Completing that task does not pass the quality gate. T082 measured the separate server below its targets for the documented workload. See [acceptance/baseline.md](acceptance/baseline.md) for evidence and remaining gates.
